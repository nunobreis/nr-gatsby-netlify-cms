import React from 'react'
import { storiesOf } from '@storybook/react'

import Subtitle from './Subtitle'

storiesOf('Subtitle', module).add('renders a subtitle', () => (
  <Subtitle>Subtitle</Subtitle>
))
