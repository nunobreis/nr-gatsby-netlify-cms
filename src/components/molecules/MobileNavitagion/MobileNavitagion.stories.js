import React from 'react'
import { storiesOf } from '@storybook/react'

import MobileNavitagion from './MobileNavitagion'

storiesOf('MobileNavitagion', module).add('renders a mobile navigation', () => (
  <MobileNavitagion />
))
