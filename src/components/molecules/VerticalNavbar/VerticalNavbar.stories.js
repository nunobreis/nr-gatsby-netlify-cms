import React from 'react'
import { storiesOf } from '@storybook/react'

import VerticalNavbar from './VerticalNavbar'

storiesOf('Vertical Navbar', module).add('renders a vertical navbasr', () => (
  <VerticalNavbar />
))
